// import { actionTypes } from "../_actions/User.action";

const initialState = {
  meta: {},
  user: {},
};

const user = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default user;
